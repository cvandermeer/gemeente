<%= render 'shared/dashboard_menu' %>

<div class='inner'>
  <div class='container'>
    <div class='row'>
      <% @reports_stasuses.each_with_index do |report_status, index| %>
        <div class='columns four'>
          <div class='panel'>
            <div class='panel-header'>
              <h5><%= Report.statuses.to_a[index][0] %></h5>
            </div>
            <div class='panel-body'>
              <table class="u-full-width">
                <thead>
                  <tr>
                    <th>Melding</th>
                    <th>Status</th>
                  </tr>
                </thead>
                <tbody>
                  <% report_status.each do |report| %>
                    <tr>
                      <td><%= link_to report.category.title, report, data: { no_turbolink: true }, target: '_blank' %></td>
                      <td>
                        <%= form_for report do |f| %>
                          <%= f.collection_select :status, Report.statuses, :first, :first %>
                        <% end %>
                      </td>
                    </tr>
                  <% end %>
                </tbody>
              </table>
            </div>
          </div>
        </div>
      <% end %>
    </div>
  </div>
</div>
