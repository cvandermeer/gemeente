<div class='inner'>
  <div class='container'>
    <p>
      <%= @newsletter.valid_from %> - <%= @newsletter.valid_until %>
    </p>
    <h2><%= @newsletter.title %></h2>
    <p>
      <%== @newsletter.body %>
    </p>
  </div>
</div>
