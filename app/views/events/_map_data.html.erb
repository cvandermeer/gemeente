<script>
  var map;
  function initMap() {
    map = new google.maps.Map(document.getElementById('map'), {
      center: {lat: 52.397, lng: 6.544},
      zoom: 7
    });
    <% events.each do |event| %>
      var el = <%== event.to_json %>
      setMarker(el)
    <% end %>
  }
</script>